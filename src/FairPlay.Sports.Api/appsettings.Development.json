{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "//": "DEV ONLY. Production must override Jwt:SigningKey via user-secrets / environment variable and never commit a real key.",
    "SigningKey": "dev-only-insecure-signing-key-change-me-0123456789"
  }
}
