# Base URL of the FairPlay.Sports backend API used by `npm run dev`.
# Must be the HTTPS endpoint: the refresh-token cookie is Secure-only and the
# API redirects HTTP -> HTTPS. Trust the dev cert once with:
#   dotnet dev-certs https --trust
VITE_API_BASE_URL=https://localhost:7090
